spring:
  application:
    name: LibraryHub

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/whatsapp
    username: postgres
    password: "root"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081